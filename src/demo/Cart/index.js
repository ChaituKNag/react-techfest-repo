import React, {Component} from 'react';

import './index.scss';

export default class CartComponent extends Component{
    render(){
        return(
            <div>
                CartComponent
                </div>
        )
    }
}