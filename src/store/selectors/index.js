export const selectUserDetails = ({ user }) => user
export const selectSearchText = ({ search }) => search
export const selectProductDetails = ({ product: { details } }) => details
export const selectProductDescription = ({ product: { description } }) => description
export const selectPage = ({ page }) => page
export const selectCart = ({ cart }) => cart
export const selectSearchResults = ({searchResults}) => searchResults